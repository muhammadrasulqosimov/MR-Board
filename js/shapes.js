import { Store } from './store.js';

export function initShapes() {
    // The shape buttons are already handled by the general data-tool in UIController.
    // This file is for any specific shape-related logic if needed.
    // We'll keep it as a placeholder.
}
